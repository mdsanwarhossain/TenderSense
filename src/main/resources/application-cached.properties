# Cached profile: replays the Day-0 disk snapshot instead of hitting live portals.
# This is the demo-day safety net -- it must work with networking disabled.
tendersense.schedule.enabled=false
tendersense.source.mode=cached
